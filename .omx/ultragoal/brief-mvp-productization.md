Complete the approved RALPLAN productization follow-up for the offline auto-trading bot MVP.

Approved scope:
- Add README onboarding for local setup, tests, CSV schema, CLI usage, report interpretation, and safety boundary.
- Add deterministic example CSV fixture and committed sample Markdown/JSON report bundle generated by the current CLI.
- Add future live/paper-trading approval-gate documentation that explicitly keeps broker/network/credential/order features out of this MVP.
- Add regression tests that verify committed sample reports are reproducible from the example CSV (normalizing only path-dependent fields) and docs preserve the no-live-trading boundary.
- Run final verification and review gates.

Hard constraints:
- Offline/local simulator only.
- No broker SDKs, network clients, credentials, .env, account reads, paper/live adapters, or live-order paths.
- No new dependencies.
- No investment advice, profit guarantees, or live-trading approval claims.

Acceptance evidence:
- README.md, examples/data/sample_ohlcv.csv, examples/reports/moving-average-report.{md,json}, docs/future-live-trading-gate.md exist.
- Tests cover examples/docs reproducibility and safety statements.
- Full pytest, ruff, mypy, compileall, tabnanny, static safety scan pass.
- Final review approves or blockers are recorded.
